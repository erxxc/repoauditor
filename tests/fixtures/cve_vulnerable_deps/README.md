# Vulnerable dependency fixture

Purpose-built manifest fixture pinning real affected PyYAML and Flask versions. This is
`kind: fixture`, not an independent repository cohort member. Exact OSV advisories and
ground-truth provenance are recorded in `expected_findings.json`; scanner output was not used
to create the labels.
