# Manufactured regex-control witnesses

These zero-token cases qualify only the deterministic JavaScript regex-witness checker.
The answer key stays outside `snapshot/`. A verified guard miss proves only regex behavior;
it does not prove that the application accepts the witness or that a security impact follows.
